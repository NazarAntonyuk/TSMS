# Використання офіційного образу Python як батьківського образу
FROM python:3.8-slim

# Встановлення робочої директорії в контейнері
WORKDIR /app

# Копіювання файлу вимог до контейнера
COPY requirements.txt .

# Встановлення бібліотек, необхідних для проекту
RUN pip install --no-cache-dir -r requirements.txt

# Копіювання усього вмісту локальної директорії в контейнер
COPY . .

# Команда для запуску веб-сервера Django
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
